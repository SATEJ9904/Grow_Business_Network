
BASE_URL=http://localhost:5001/api/
# BASE_URL=https://api.gbnsocialassociations.in/api/

# TODO: this still points at a developer's personal LAN IP (only reachable on
# that one machine's WiFi) — the AI website-generation feature is broken for
# every real user until this is replaced with the actual deployed address of
# the Python generation service.
PYTHON_SERVICE_URL=http://localhost:5002